<%= form_with model:[book,book_comment], local: false  do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows:5, class:"form-control comment-textarea"%>
  </div>
  <div class="form-group">
    <%= f.submit "送信", class:"btn btn-secondary"%>
  </div>
<% end %>
